import thunk from 'redux-thunk';
import fetchMock from 'fetch-mock';

describe('STORES › ACTIONS › claims actions', () => {
  afterEach(() => {
    fetchMock.restore();
  });

  const storeData = {};

  describe('getClaims', () => {
    it('should dispatch ', async () => {});
  });
});
