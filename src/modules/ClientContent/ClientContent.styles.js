const styles = (theme) => ({
  header: {
    marginBottom: theme.spacing(4),
  },
  selection: {
    width: '100% !important',
    maxWidth: 300,
    marginTop: theme.spacing(1),
  },
});

export default styles;
