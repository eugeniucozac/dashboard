const styles = (theme) => ({
  paper: {
    padding: '6px 16px',
  },
  secondaryTail: {
    backgroundColor: theme.palette.error.main,
  },
  leftListItem: { flexDirection: 'row-reverse' },
});

export default styles;
