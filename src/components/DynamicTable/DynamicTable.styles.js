const styles = (theme) => ({
  root: {
    marginBottom: theme.spacing(4),
  },
  tableHead: {
    '& th': {
      fontWeight: theme.typography.fontWeightBold,
    },
  },
});

export default styles;
